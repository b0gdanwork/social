import { profileActions, profileReducer } from './model/slice/userSlice';
import { type ProfileSchema, type ProfileT } from './model/types/profileSchema';

export { profileActions, profileReducer, type ProfileSchema, type ProfileT }