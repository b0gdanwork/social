/* eslint-disable i18next/no-literal-string */
import { render } from '@testing-library/react'
import Dropdawn from './Dropdawn'

describe('test Dropdawn render', () => {
  test('', () => {
    // const { container } = render(<Dropdawn>Dropdawn</Dropdawn>)

    // expect(container.getElementsByTagName('Dropdawn')[0]).toBeInTheDocument()
  })
})
